import XCTest
@testable import ExtensionHelper3

final class ExtensionHelper3Tests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExtensionHelper3().text, "Hello, World!")
    }
}
